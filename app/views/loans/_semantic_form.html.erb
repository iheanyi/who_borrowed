<h1>Create New Loan Form here</h1>
  <h3>Parameters for Loan Form (nested forms)</h3>
  <ul>
    <li>New/Existing Item Form, should have item name, specify a picture to upload</li>
    <li>New/Existing Borrower Form, borrower name (autocomplete with Facebook friends name and the likes)</li>
    <li>Date of Loan (add a checkbox for unknown)</li>
    <li>Requested Return Date (add a checkbox for indeterminate/whenever)</li>
    <li>If there is a requested return date, then show a checkbox for an e-mail reminder.</li>
    <li>No requested return date? Then the checkbox should state, "E-mail me in N [days, weeks,months] from [today, loan date]</li>
    <li>And Submit Button</li>
</ul>

<div class=" card col-md-6 col-md-offset-3">
  <%= semantic_form_for @loan do |f| %>

  <%= f.inputs :name => "Borrower", :for => :borrower do |borrower_form| %>
    <%= borrower_form.input :name, :label => "Borrower's name" %>
  <% end %>

  <div class="row">
    <div class="col-md-4">
      <%= f.input :loaned_on, :label => "Date of Loaning", :as => :date_picker, :class => "loan-date" %>
    </div>

    <div class="col-md-4">
      <%= f.input :return_by, :label => "Requested Return Date", :as => :date_picker, :class => "return-date" %>
    </div>
  </div>

  <%= f.inputs :name => "Item", :for => :item  do |item| %>
    <%= item.input :name,  :label => "Name of Item Borrowed" %>
    <%= item.input :image, :label => "Item Image", :as => :file, :required => false %>
  <% end %>

  <!--<%= semantic_fields_for :borrower do |borrower| %>
    <%= borrower.inputs :name, :name=> "Borrower" %>
  <% end %>-->


  <%= f.action :submit, :class => "btn btn-primary" %>
<% end %>
</div>